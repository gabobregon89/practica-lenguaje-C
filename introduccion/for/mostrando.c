#include <stdio.h>

int main() {

    /* 4.2 Mostrar los números desde el 32 hasta el 0, mientras no se pulse una tecla. */

    for (int i = 32; i >= 0; i--) {
        printf("%d\n", i);
    }

    return 0;
}